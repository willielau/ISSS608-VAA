---
title: "Hands-on Exercise 1"
author: "Willie Lau"
---

# Getting Started

## Install and launching R packages

The code chunk below uses p_load() of pacman package to check if tidyverse packages have been installed in the computer. If they are, then they will be launched into R.

```{r}
pacman::p_load(tidyverse)
```

## Importing data

```{r}
exam_data <- read_csv("data/Exam_data.csv")
```

# Plotting a simple bar chart (code chunk only)

```{r}
#| eval: false
ggplot(data = exam_data,
       aes(x = RACE)) +
  geom_bar()
```

# Plotting a simple bar chart (chart only)

```{r}
#| echo: false
ggplot(data = exam_data,
       aes(x = RACE)) +
  geom_bar()
```
